<Window x:Class="ebook.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:pdb="clr-namespace:Shell.Pdb;assembly=Shell"
        Title="MainWindow" Height="500" Width="800">
    <Window.Resources>
        <ResourceDictionary>
            <DataTemplate DataType="pdb:MobiFile">
                <Grid>
                    <TextBlock Text="{Binding Title}"/>
                </Grid>
            </DataTemplate>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <TextBox 
            HorizontalAlignment="Left" 
            Height="23" Margin="20,20,0,0" 
            TextWrapping="Wrap" VerticalAlignment="Top" Width="250"
            Text="{Binding ImportFolderPath}"/>
        <Button 
            Content="Import" 
            HorizontalAlignment="Left" 
            Margin="275,20,0,0" VerticalAlignment="Top" Width="75"
            Command="{Binding ImportCommand}"
            />
        <DataGrid
            Margin="20,50,20,20"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            CanUserResizeRows="False"
            ItemsSource="{Binding MobiFileList}"
            GridLinesVisibility="None"
            AlternatingRowBackground="AliceBlue"
            IsTextSearchEnabled="True"
            
            >
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Title}" Header="Title"/>
                <DataGridTextColumn Binding="{Binding Author}" Header="Author"/>
                <DataGridTextColumn Binding="{Binding Isbn}" Header="ISBN"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
